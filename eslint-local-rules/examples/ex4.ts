/* eslint-disable unicorn/prefer-module,  global-require */

import a, { a as d2, a1, a2 } from 'fs';
import * as d1 from 'fs';

const b = require('fs');
const {
    b1, b2, b3: d3, b4: { d4, d5 },
} = require('fs');
