// /* eslint-disable */
/* eslint local-rules/context: "error" */

// const A = () => {};

// const B = () => new Date();

const F = async () => {
    // console.info(100);
};

class C {
    // d: Date;
    // constructor() {
    //     this.d = new Date();
    // }

    /* async */ M() {
        // console.info(1000);

        // () => F()
        // ctx.doSync(() => F());

        // // setTimeout(F, 10);
        // setTimeout(F, 10);
        // // setInterval(F, 10);
        // setInterval(F, 10);
        // // setTimeout(await F(), 10);
        // setTimeout(ctx.doSync(() => F()), 10);
        // // setTimeout(ctx.doHandleError(F()), 10);
        // setTimeout(ctx.doHandleError(F()), 10);
        // // setTimeout(await ctx.doHandleError(F()), 10);
        // setTimeout(await ctx.doHandleError(F()), 10);
        // // setTimeout(ctx.doHandleError(() => { F(); }), 10);
        // setTimeout(ctx.doHandleError(async () => { await F(); }), 10);
        // // setTimeout(await ctx.doHandleError(async () => { F(); }), 10);
        // setTimeout(await ctx.doHandleError(async () => { F(); }), 10);
    }
}
